__pycache__/
*.pyc
.env
.env.*
!.env.example
settings.local.json
.DS_Store
Thumbs.db
*.log
*.tmp

# Wrapper-lane is stateless (re-parses on disk every call). No daemon, no SQLite, no persistent
# graph artefact — nothing here to ignore for the canonical lane. The .code-review-graph/ entry
# below is preserved while developer machines may still have legacy dirs from the pre-#445
# MCP-era server (deprecated; safe to delete locally).
.code-review-graph/
